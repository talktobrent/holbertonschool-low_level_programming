/**
* _mul - multiply
* @a: First operand
* @b: Second operand
*
* Description: multiplies two digits
* Return: product of a and b
*/

int mul(int a, int b)
{
	long int ab = (a * b);

	return (ab);
}
