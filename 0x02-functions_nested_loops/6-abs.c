

/**
* _abs - absolute value
* @x: number checked
*
* Description: extracts abolute value
* Return: absolute value of x
*/

int _abs(int x)
{
	if (x >= 0)
		(x = x);

	else
	(x = (x / -1));

	return (x);
}
